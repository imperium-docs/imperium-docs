name: Atlas Autopublish (Legacy Disabled)

on:
  workflow_dispatch:
    inputs:
      slot:
        description: "Optional slot override (morning | noon | night)"
        required: false
        type: choice
        options:
          - morning
          - noon
          - night

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Atlas Autopublish is disabled. Use Atlas Cron."
