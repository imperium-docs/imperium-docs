name: Atlas Ingest (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Atlas Ingest is disabled. Use Atlas Cron."
